Run a fast validation test on the ontology without full reasoning or report generation.

Execute: `cd src/ontology && make test_fast`

Useful for quick checks during active development. For comprehensive validation, use `/validate` instead.